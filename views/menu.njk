<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/recetas">Mi Aplicación de Recetas</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <a class="nav-link" href="/recetas">Listado de Recetas</a>
        </li>
        {% if (session and session.usuario) %}
        <a class="btn btn-dark" href="/auth/logout">Logout</a>
        {% else %}
        <a class="btn btn-dark" href="/auth/login">Login</a>
        {% endif %}
        {% if (session and session.usuario) %}
        <li class="nav-item">
          <a class="nav-link" href="/recetas/nueva">Nueva Receta</a>
        </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>